[project]
name = "pyqtweb"
version = "0.0.0"
description = "Demo PySide QtWebView application"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
	"pyside6",
]

[tool.setuptools.packages.find]
exclude = [
	"examples*",
	"docs*"
]

